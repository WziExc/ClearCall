name: signaling_server
description: ClearCall WebSocket 信令中继服务器 — 极简房间消息转发
version: 0.1.0

environment:
  sdk: ^3.0.0

dependencies:
  shelf: ^1.4.0
  shelf_web_socket: ^2.0.0
  web_socket_channel: ^3.0.0
